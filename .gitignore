# Build output
/bin/
*.exe

# Large raw video corpus (referenced by manifest, never committed) — see CLAUDE.md §7
/corpus/*
!/corpus/manifest/

# Go
*.test
*.out
coverage.*

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local scratch (spike harnesses, reference clones)
/tmp/

# Dev-time ML training environment + artifacts (models are shipped via data/, not ml/)
/ml/.venv/
/ml/out*/
/ml/__pycache__/

# Raw unlabeled capture videos + blind-transcription proposals (machine-local)
/rawvid/

# GUI frontend (Wails v2 + Svelte) — generated artifacts
/gui/frontend/node_modules/
/gui/frontend/dist/*
!/gui/frontend/dist/.gitkeep
/gui/frontend/wailsjs/
/gui/frontend/package.json.md5
/gui/build/
