// Package e2e holds cross-package integration tests that wire the perception +
// inference front-half together (capture → stableframe → calibrate → boardstate
// → boarddiff → engine). It contains no production code.
package e2e
